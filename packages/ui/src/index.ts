/**
 * @papopro/ui — placeholder de M1.
 *
 * M2 popula esse package com shadcn primitives (Button, Input, Dialog…),
 * componentes de domínio (StatusDot, TemperatureBadge, EmptyState…),
 * tokens via CSS custom properties e suporte a dark mode.
 */
export { cn } from './utils/cn';
